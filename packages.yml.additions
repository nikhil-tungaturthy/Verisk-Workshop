# =============================================================================
# Add these packages to your existing packages.yml.
# If you don't have a packages.yml at the project root, create one with the
# contents below.
# =============================================================================

packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]

# After adding, run:
#   dbt deps
